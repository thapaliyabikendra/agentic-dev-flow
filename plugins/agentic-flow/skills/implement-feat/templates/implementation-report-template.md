# Implementation Report — {{FEATURE_TITLE}}

> Auto-generated by `implementation-feed` on {{TIMESTAMP_ISO}}.
> Do not edit this file by hand. Regenerate by rerunning the skill against the same feature slug.

---

## Metadata

| Field | Value |
|---|---|
| Feature slug | `{{FEATURE_SLUG}}` |
| Feature PascalCase | `{{FEATURE_PASCAL}}` |
| Feat Spec source | [`{{FS_WIKI_PATH}}`]({{FS_WIKI_URL}}) |
| Generator version | `implementation-feed v{{SKILL_VERSION}}` |
| Sub-agent run IDs | {{SUBAGENT_RUN_IDS}} |
| Reconciliation approved at | {{RECONCILIATION_APPROVED_AT}} |
| Artifact plan approved at | {{PLAN_APPROVED_AT}} |
| Build verified at | {{BUILD_VERIFIED_AT}} |
| CLAUDE.md contract version | {{CLAUDE_MD_HASH}} |

---

## Reconciliation Summary

The skill first scanned the repository, then compared every Feature-Spec element against the scan. Each element received one of four decisions:

| Decision | Count | Meaning |
|---|---|---|
| REUSE_AS_IS | {{REUSE_COUNT}} | Existing file matches the FS; no I/O performed |
| UPDATE_IN_PLACE | {{UPDATE_COUNT}} | Existing file extended via `str_replace` edits (additive only) |
| CREATE_NEW | {{CREATE_COUNT}} | No suitable existing file; new file written |
| CONFLICT (resolved) | {{CONFLICT_RESOLVED_COUNT}} | Scout found a conflicting candidate; user chose resolution at Phase 7 |

{{#IF_ANY_CONFLICTS}}
**User-resolved conflicts:**
{{#EACH_CONFLICT_RESOLUTION}}
- `{{FS_ELEMENT}}` — original conflict: {{CONFLICT_CODE}}. Chosen resolution: **{{RESOLUTION}}**.
{{/EACH_CONFLICT_RESOLUTION}}
{{/IF_ANY_CONFLICTS}}

### Command realization breakdown

Every Command in the FS was assigned a realization type (not all Commands become AppService methods).

| Realization type | Count | Projects touched |
|---|---|---|
| AppService | {{APPSERVICE_COUNT}} | {{APPSERVICE_PROJECTS}} |
| BackgroundJob | {{BGJOB_COUNT}} | {{BGJOB_PROJECTS}} |
| HostedService | {{HOSTEDSVC_COUNT}} | {{HOSTEDSVC_PROJECTS}} |
| HubMethod | {{HUB_COUNT}} | {{HUB_PROJECTS}} |
| EventHandler | {{EVENTHANDLER_COUNT}} | {{EVENTHANDLER_PROJECTS}} |
| CliCommand | {{CLI_COUNT}} | {{CLI_PROJECTS}} |

{{#IF_REALIZATION_OVERRIDES}}
**User-overridden realizations** (Command lacked an `**Execution model:**` field; user chose at Phase 5):
{{#EACH_REALIZATION_OVERRIDE}}
- `{{COMMAND_NAME}}` → **{{CHOSEN_REALIZATION}}**.
{{/EACH_REALIZATION_OVERRIDE}}
{{/IF_REALIZATION_OVERRIDES}}

---

## Repo-Scout Snapshot

Before any planning, `repo-scout` ran parallel per project.

| Metric | Value |
|---|---|
| Solution file | `{{SOLUTION_PATH}}` |
| Projects enumerated | {{PROJECT_COUNT}} |
| Projects scanned (feature-relevant) | {{FEATURE_RELEVANT_PROJECT_COUNT}} |
| Candidates indexed | {{CANDIDATE_COUNT}} |
| Strong-match candidates | {{STRONG_MATCH_COUNT}} |
| Weak-match candidates | {{WEAK_MATCH_COUNT}} |

### Supported realizations in this solution

| Realization | Supported | Source of evidence |
|---|---|---|
| AppService | {{SUPPORTS_APPSERVICE}} | Application project present |
| BackgroundJob | {{SUPPORTS_BGJOB}} | {{BGJOB_EVIDENCE}} |
| HostedService | {{SUPPORTS_HOSTEDSVC}} | {{HOSTEDSVC_EVIDENCE}} |
| HubMethod | {{SUPPORTS_HUB}} | {{HUB_EVIDENCE}} |
| EventHandler | {{SUPPORTS_EVENTHANDLER}} | {{EVENTHANDLER_EVIDENCE}} |
| CliCommand | {{SUPPORTS_CLI}} | {{CLI_EVIDENCE}} |

{{#IF_SCOUT_WARNINGS}}
**Scout warnings** (surfaced at Phase 3, not blocking):
{{#EACH_SCOUT_WARNING}}
- `{{CODE}}` — {{MESSAGE}}
{{/EACH_SCOUT_WARNING}}
{{/IF_SCOUT_WARNINGS}}

---

## Inventory by Decision

### REUSE_AS_IS ({{REUSE_COUNT}})

Existing files that already satisfy the FS. The skill read them, verified their shape, and referenced them in the traceability matrix — no writes, no edits.

| FS element | Kind | File | Project |
|---|---|---|---|
{{#EACH_REUSE}}
| `{{FS_TYPE}} {{FS_NAME}}` | {{ARTIFACT_KIND}} | `{{FILE_PATH}}` | `{{PROJECT}}` |
{{/EACH_REUSE}}

### UPDATE_IN_PLACE ({{UPDATE_COUNT}})

Existing files extended via `str_replace` edits. Every edit was additive (no removals, type changes, or visibility changes).

{{#EACH_UPDATE_GROUP}}
#### `{{FILE_PATH}}`

**Intent:** {{UPDATE_INTENT_ONE_LINE}}
**FS sources:** {{FS_SOURCES_LIST}}
**Edits applied:** {{EDIT_COUNT}}

```diff
{{UNIFIED_DIFF}}
```

{{/EACH_UPDATE_GROUP}}

{{#IF_NO_UPDATES}}
No UPDATE_IN_PLACE decisions were made.
{{/IF_NO_UPDATES}}

### CREATE_NEW ({{CREATE_COUNT}})

New files written from templates.

#### Domain layer

| FS element | Target file | Kind |
|---|---|---|
{{#EACH_CREATE_DOMAIN}}
| `{{FS_TYPE}} {{FS_NAME}}` | `{{FILE_PATH}}` | {{ARTIFACT_KIND}} |
{{/EACH_CREATE_DOMAIN}}

#### Domain.Shared layer

{{#EACH_CREATE_DOMAIN_SHARED}}
- `{{FILE_PATH}}` — {{ARTIFACT_KIND}} ← FS: {{FS_SOURCES}}
{{/EACH_CREATE_DOMAIN_SHARED}}

#### Application.Contracts layer

{{#EACH_CREATE_APPCONTRACTS}}
- `{{FILE_PATH}}` — {{ARTIFACT_KIND}} ← FS: {{FS_SOURCES}}
{{/EACH_CREATE_APPCONTRACTS}}

#### Application layer

{{#EACH_CREATE_APPLICATION}}
- `{{FILE_PATH}}` — {{ARTIFACT_KIND}} ← FS: {{FS_SOURCES}}
{{/EACH_CREATE_APPLICATION}}

#### EntityFrameworkCore layer

{{#EACH_CREATE_EFCORE}}
- `{{FILE_PATH}}` — {{ARTIFACT_KIND}} ← FS: {{FS_SOURCES}}
{{/EACH_CREATE_EFCORE}}

{{#IF_AUXILIARY_CREATES}}
#### Auxiliary projects

{{#EACH_CREATE_AUXILIARY}}
- `{{FILE_PATH}}` ({{PROJECT_KIND}}) — {{ARTIFACT_KIND}} ← FS: {{FS_SOURCES}}
{{/EACH_CREATE_AUXILIARY}}
{{/IF_AUXILIARY_CREATES}}

---

## Traceability Matrix

Forward (FS element → artifacts) and backward (artifact → FS element) coverage verified by `traceability-validator`.

### Forward coverage

| FS element | Decision | Primary artifact | Additional artifacts |
|---|---|---|---|
{{#EACH_FS_ELEMENT}}
| `{{TYPE}} {{NAME}}` | {{DECISION}} | `{{PRIMARY_ARTIFACT}}` | {{ADDITIONAL_ARTIFACTS}} |
{{/EACH_FS_ELEMENT}}

### Backward provenance

Every new or edited artifact maps back to at least one FS element. Validator reported **{{BACKWARD_ORPHAN_COUNT}}** orphans. Full mapping in `{{TRACEABILITY_JSON_PATH}}`.

---

## Convention Compliance

Checked by `traceability-validator` against CLAUDE.md.

| Convention | Compliant | Notes |
|---|---|---|
| Validation library ({{VALIDATION_LIB}}) | {{VALIDATION_COMPLIANT}} | {{VALIDATION_NOTES}} |
| Mapping library ({{MAPPING_LIB}}) | {{MAPPING_COMPLIANT}} | {{MAPPING_NOTES}} |
| Sorting strategy ({{SORTING_STRATEGY}}) | {{SORTING_COMPLIANT}} | {{SORTING_NOTES}} |
| Tenancy model ({{TENANCY_MODEL}}) | {{TENANCY_COMPLIANT}} | {{TENANCY_NOTES}} |
| Namespace prefix ({{ROOT_NS}}) | compliant | All new files use the declared prefix |

{{#IF_DEFAULTS_USED}}
**CLAUDE.md optional fields that used defaults:**
{{#EACH_DEFAULT_USED}}
- `{{FIELD}}` → `{{DEFAULT_VALUE}}`.
{{/EACH_DEFAULT_USED}}
{{/IF_DEFAULTS_USED}}

---

## Build Status

| Metric | Value |
|---|---|
| Command | `{{BUILD_COMMAND}}` |
| Exit code | {{BUILD_EXIT_CODE}} |
| Errors | {{BUILD_ERROR_COUNT}} |
| Warnings | {{BUILD_WARNING_COUNT}} |
| Repair iterations used | {{REPAIR_ITERATIONS}} of 3 |
| Duration | {{BUILD_DURATION}} |

{{#IF_REPAIR_USED}}
### Repair iteration log

{{#EACH_REPAIR_ITERATION}}
**Iteration {{N}}** — {{ERROR_COUNT}} errors. Fixes applied:
{{#EACH_FIX}}
- `{{FILE_PATH}}`: {{FIX_SUMMARY}}
{{/EACH_FIX}}
{{/EACH_REPAIR_ITERATION}}
{{/IF_REPAIR_USED}}

---

## Manual Next Steps

The skill stops here. The steps below must be run by a human.

### 1. Database migration

New or modified EF configurations require a migration. This skill does **not** run `dotnet ef` commands.

```bash
cd {{EFCORE_DBMIGRATIONS_PROJECT_PATH}}
dotnet ef migrations add {{FEATURE_PASCAL}}Feature --context {{DBCONTEXT_TYPE_NAME}}
dotnet ef database update --context {{DBCONTEXT_TYPE_NAME}}
```

Inspect the generated migration before applying to production.

### 2. Permission grants

Permissions are defined but not yet granted. Grant to roles via ABP's permission management UI or in a seed contributor:

{{#EACH_NEW_PERMISSION}}
- `{{PERMISSION_CONSTANT}}` → grant to role(s): {{SUGGESTED_ROLES}}
{{/EACH_NEW_PERMISSION}}

### 3. Background job scheduling

{{#IF_ANY_BGJOBS}}
This feature includes background jobs. The skill emitted the job classes but did **not** register them with a scheduler. Register in your deployment pipeline:

{{#EACH_BGJOB}}
- `{{JOB_CLASS}}` — suggested schedule per FS: {{SUGGESTED_SCHEDULE}}
{{/EACH_BGJOB}}
{{/IF_ANY_BGJOBS}}

{{#IF_NO_BGJOBS}}
No background jobs in this feature.
{{/IF_NO_BGJOBS}}

### 4. Hub client registration

{{#IF_ANY_HUBS}}
New SignalR hub methods were created. Update client code to subscribe:

{{#EACH_HUB_METHOD}}
- `{{HUB_CLASS}}.{{METHOD}}` — client subscription required
{{/EACH_HUB_METHOD}}
{{/IF_ANY_HUBS}}

### 5. Tests

The skill generates implementation, not tests. Recommended test coverage:

- Unit tests for domain methods on new or updated aggregates ({{UPDATED_AGGREGATES_LIST}}).
- Integration tests for new AppService methods ({{NEW_APPSERVICE_METHODS_LIST}}).
- Idempotency tests for background jobs ({{NEW_JOBS_LIST}}) — double-dispatch must not corrupt state.
- End-to-end authorization smoke tests for new permissions.

### 6. Code review checklist

Before merging:

- [ ] Migration reviewed and approved by DBA / tech lead.
- [ ] Permissions granted to the right roles.
- [ ] Feat Spec and updated DDD pages re-read against final code.
- [ ] Integration tests pass.
- [ ] Authorization tested with a non-privileged user.
- [ ] Tenant isolation tested (if `IMultiTenant` applies).
- [ ] Background jobs scheduled in deployment config.
- [ ] SignalR clients updated (if hub methods added).

### 7. Commit

Suggested commit message:

```
feat({{FEATURE_SLUG}}): implement Feat Spec v{{FS_VERSION}}

Reconciliation: {{REUSE_COUNT}} reused, {{UPDATE_COUNT}} updated, {{CREATE_COUNT}} created.
Realizations: {{REALIZATION_SUMMARY_INLINE}}.

FS: {{FS_WIKI_URL}}
Report: {{REPORT_RELATIVE_PATH}}
```

---

## Files Produced or Modified

### Created files ({{CREATE_COUNT}})

{{#EACH_CREATED_FILE}}
- `{{FILE_PATH}}`
{{/EACH_CREATED_FILE}}

### Edited files ({{UPDATE_COUNT}})

{{#EACH_UPDATED_FILE}}
- `{{FILE_PATH}}` — {{EDIT_COUNT}} edits
{{/EACH_UPDATED_FILE}}

### Files referenced (REUSE) but not modified ({{REUSE_COUNT}})

{{#EACH_REUSED_FILE}}
- `{{FILE_PATH}}` — satisfies {{FS_SOURCES_SUMMARY}}
{{/EACH_REUSED_FILE}}

---

## Rerun Policy

If you need to regenerate this feature:

1. Confirm the Feat Spec is still locked (no placeholders, no blocking Conflicts).
2. Rerun `implementation-feed` with the same slug.
3. The repo scout will re-scan; the reconciler will re-decide based on the current state (files you may have edited by hand since last run).
4. New reconciliation plan will likely show more REUSE / UPDATE decisions than the first run — that's expected.
5. Approve the reconciliation and artifact plan as before.

Nothing in this skill requires manual cleanup before rerunning. It is idempotent against its own outputs.
